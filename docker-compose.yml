version: '3.8'

services:
  postgres:
    image: postgres:15
    container_name: zombieland-postgres
    environment:
      POSTGRES_USER: zombieland
      POSTGRES_PASSWORD: zombieland_password
      POSTGRES_DB: zombieland_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
